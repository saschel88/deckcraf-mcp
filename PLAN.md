# План дальнейшей работы

> **Исполнителю:** задачи идут по порядку, каждая заканчивается зелёными
> проверками и коммитом. Отметки `- [ ]` — для отслеживания.

**Цель:** довести библиотеку до состояния, когда любой из 52 элементов
принимает данные пользователя и честно сообщает, что с ними сделал.

**Подход:** данные заходят вторым аргументом в саму функцию отрисовки.
Подставить текст снаружи нельзя: у диаграмм подпись и геометрия берутся
из одного числа. Без данных разметка обязана совпадать с прежней побайтово —
это и есть страховка от того, что 676 образцов поедут.

**Основа:** Node ≥18, `@modelcontextprotocol/sdk`, транспорт stdio.
Ни браузера, ни сборщика.

**Спецификация:** `ТЗ.md` — постановка и принятые решения.
`ГДЕ-ЧТО.md` — карта файлов. `ЗАМЕТКИ.md` — состояние работ.

## Порядок работ

Пересмотрен 22 августа, после того как нашлась причина, общая для нескольких
задач. Номера задач оставлены прежними — переставлены приоритеты, а не заголовки.

| # | задача | чьё лицо | почему здесь |
|---|---|---|---|
| **15** | ужатый кегль выбрасывается | зритель | корень: 73 поля из 90 вылезают за холст на своём же пределе. Починка не меняет ни байта образцов |
| **14** | разбор живого прогона | зритель | следствия пятнадцатой плюс своё. Пределы перемерять только после неё |
| **12** | элементу нечем сказать про беду | модель, зритель | молчаливая потеря данных: несходящийся итог, зажатые значения, невидимые потомки |
| **10** | поиск понимает человека | модель | 15 попаданий из 25. Лекарство лежит в самой спецификации |
| **16** | мера текста: регистр | зритель | недобор 19% на прописных, а капс у 7 стилей из 13 — половина библиотеки |
| **17** | политика автофита числами | зритель, ведущий | пять механизмов применяются вразнобой; отрасль задаёт режим заранее, мы — нигде |
| **9** | шкала дельты в retro | зритель | один элемент в одном стиле. Узко, но картинка спорит с текстом |
| **13** | сплошная сверка композиции | модель | нужна ровно настолько, насколько нужен путь «модель рисует сама» |
| **11** | путь Claude Design | модель | шаг ноль требует самого Design, его на машине нет |

Задачи 16 и 17 пришли из разведки по чужим библиотекам 22 августа
и встают сразу после 14: обе про то же — текст, который не влезает.
Шаг 1 задачи 16 (четыре коэффициента вместо одного) дёшев и снимает
единственный найденный системный недобор.

**Правило, по которому расставлено.** У каждой задачи названо лицо, ради
которого она делается. Если лицо назвать нельзя — задача, вероятно, лишняя:
так с плана сняли переезд на HTTP. Выше стоят те, где страдает **зритель
слайда**, потому что его ошибку никто не поймает: он видит готовую картинку
и верит ей.

## Общие ограничения

Действуют в каждой задаче без напоминания.

- **`src/engine.js` не трогать.** Токены, стили, помощники — готовы.
- **Побайтовая совместимость.** `npm run check` обязан говорить
  «разошлись 0». Если разошлось хоть что-то — правка неверна, а не эталон.
- **`npm run baseline` не запускать.** Он затирает доказательство
  совместимости, и заметить это будет уже нечем.
- **Имена полей — из договора** в `spec/elements.json`, не выдумывать.
  Посмотреть договор: `node -e "const j=require('./spec/elements.json');
  console.log(JSON.stringify(Object.values(j['элементы']).find(э=>э.id==='ИД')['данные'],null,1))"`
- **Пороги выводить из геометрии, а не подбирать константой.** На этом
  обожглись дважды: рост холста упирал содержимое в кромку, а порог,
  посчитанный для полосы в 9px, ломался у retro с полосой в 10px.
- **Новые файлы называть латиницей.** Содержимое остаётся русским.
- **Четыре проверки после каждой задачи:**
  `npm run check` · `npm run data` · `npm run smoke` · `npm run host`

## Общий порядок перевода элемента

Отработан на 29 функциях. Повторяется в задачах 2–7, здесь описан один раз.

1. Собрать зашитое содержимое в один массив в начале функции.
2. Добавить второй аргумент: `function elX(s, дан)`.
3. Подставить через `ряды(дан && дан.поле, образец, максимум)` для массивов
   и `строка(дан && дан.поле, образец)` для строк. Образец остаётся значением
   по умолчанию — иначе побайтовое совпадение потеряется.
4. Подписи пропустить через `ужать` (одна строка) либо `ужатьНабор`
   (единый кегль на ряд).
5. Если число рядов переменное — посчитать визуальный низ, звать `рост()`,
   закрывать `wrapSvgH(s, o, hh)` вместо `wrapSvg`, фон рисовать
   `frameH(s, hh)` вместо `frame`. При hh = 190 они совпадают побайтово.
6. `npm run check` — «разошлись 0».
7. Добавить элемент в `Д` внутри `tools/data-check.js` и прогнать
   `npm run data`. Подписи брать длинные и русские — «Затраты на персонал»,
   «НАО Госкорпорация»: короткие ничего не проверяют.

Сервер править не нужно. Он сличает результат с отрисовкой без данных
и снимает предупреждение сам, как только элемент начинает данные принимать.

---

### Задача 1. `describe` становится пригодным для самостоятельной отрисовки

Живая проверка агентом без контекста: «`describe` обещает композицию
в относительных величинах, а даёт словесное описание без чисел и без примера
заполнения. Форму массива я вывел логически, а не прочитал».

Это ломает заявленное назначение инструмента — отдать Claude Design всё
нужное, чтобы нарисовать элемент самому.

**Файлы:**
- Изменить: `spec/elements.json` — поля `композиция` и `данные`
- Изменить: `src/index.js` — обработчик `делатьDescribe`
- Изменить: `tools/smoke.js` — новые пробы

- [x] **Шаг 1. Проба, что композиция содержит числа**

```js
const комп = JSON.stringify(опис['композиция']);
проба('композиция задана числами, а не словами', /\d/.test(комп), комп.slice(0, 80));
```

- [x] **Шаг 2. Убедиться, что проба падает**

`npm run smoke` → СБОЙ на `fmatrix`: композиция состоит из
«заголовки колонок сверху, строки признаков, отметки в ячейках».

- [x] **Шаг 3. Дописать числа в композицию**

Композиция описывается долями холста 300×190, а не пикселями — элемент
растёт, и пиксели соврут. Для `fmatrix`:

```json
"композиция": {
  "структура": "заголовки колонок сверху, строки признаков, отметки в ячейках",
  "сетка": { "подпись_признака": "0.09–0.42 ширины", "колонки": "три равные доли 0.42–0.92",
             "первая_строка": "0.34 высоты", "шаг_строки": "0.10 высоты" },
  "доминанта": "первая колонка — своё решение",
  "чего_не_делать": ["не ставить своё решение не первым",
                     "не использовать больше двух состояний отметки"]
}
```

Пройти так по всем 52. Числа снимать с готовой отрисовки:
`node -e "..."` c `draw` и замером координат, а не на глаз.

- [x] **Шаг 4. Дописать предельные длины подписей в договоры**

Агенты подбирали вслепую — «десять знаков проходит, четырнадцать обрезается».
У `kpi` и `column` длина уже указана, у большинства нет. Значение брать
измерением: подавать в `draw` строки нарастающей длины, пока в предупреждениях
не появится «обрезаны».

- [x] **Шаг 5. `describe` отдаёт пример заполнения**

В ответ добавить `пример_данных` — тот самый объект, которым элемент
рисуется по умолчанию. Он уже есть в коде функции отрисовки; вынести
его в отдельную экспортируемую константу и отдавать.

- [x] **Шаг 6. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "describe отдаёт числа, пределы подписей и пример данных"
```

---

### Задача 2. Шесть элементов без массивов

Самые простые: данные — только строки, геометрия от них не зависит,
рост холста не нужен.

**Файлы:**
- Изменить: `src/elements.js` — `elKpi`, `elBigNum`, `elDivider`,
  `elThanks`, `elSpeaker`, `elGauge`
- Изменить: `tools/data-check.js` — шесть записей в `Д`

**Договоры:**

| ид | функция | поля |
|---|---|---|
| `kpi` | `elKpi` | `label` (до 20), `value` (до 8), `delta?` |
| `bignum` | `elBigNum` | `eyebrow`, `value`, `caption`, `delta?` |
| `divider` | `elDivider` | `eyebrow`, `num`, `title`, `sub?` |
| `thanks` | `elThanks` | `title`, `name`, `org`, `contact` |
| `speaker` | `elSpeaker` | `initials` (две буквы), `name`, `role`, `org`, `line1`, `line2` |
| `gauge` | `elGauge` | `title`, `value` (число 0–100), `caption` |

- [x] **Шаг 1. Перевести `elKpi`**

```js
function elKpi(s, дан){
  const label = строка(дан && дан.label, 'Выручка');
  const value = строка(дан && дан.value, '₸2.4B');
  const delta = строка(дан && дан.delta, '▲ 23% г/г');
  ...
}
```

Подписи пропустить через `ужать`: `value` кладётся крупным кеглем и вылезает
за карточку первым.

- [x] **Шаг 2. `npm run check` — «разошлись 0»**

Если разошлось — образец подставлен не тот, что был зашит. Сверить с
`git diff` по строковым литералам.

- [x] **Шаг 3. Повторить для остальных пяти**

`gauge` — единственный с числом: `value` управляет углом стрелки,
проверить границы 0–100 и поведение за ними.
`speaker` — `initials` рисуется в кружке фиксированного размера,
больше двух знаков не влезет: ужимать нельзя, надо резать.

- [x] **Шаг 4. Добавить шесть записей в `tools/data-check.js`**

```js
kpi:      { label: 'Затраты на персонал', value: '₸12.4B', delta: '▲ 23% г/г' },
bignum:   { eyebrow: 'Экономия за год', value: '₸2.4B', caption: 'по 14 ведомствам' },
divider:  { eyebrow: 'раздел', num: '02', title: 'Автоматизация закупок' },
thanks:   { title: 'Спасибо', name: 'Алексей Шестак', org: 'НАО Госкорпорация',
            contact: 'a.shestak@qits.one' },
speaker:  { initials: 'АШ', name: 'Алексей Шестак', role: 'Технический директор',
            org: 'НАО Госкорпорация', line1: '12 лет в интеграции', line2: 'Астана' },
gauge:    { title: 'Готовность контура', value: 78, caption: 'от плана на квартал' }
```

- [x] **Шаг 5. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Шесть элементов без массивов принимают данные"
```

---

### Задача 3. Пять элементов с одним массивом, простая геометрия

**Файлы:**
- Изменить: `src/elements.js` — `elChapter`, `elColumn`, `elStatCard`,
  `elRing`, `elVenn`
- Изменить: `tools/data-check.js`

**Договоры:**

| ид | функция | поля |
|---|---|---|
| `chapter` | `elChapter` | `eyebrow`, `title`, `lines`, `meta` |
| `column` | `elColumn` | `title` (до 30), `bars[]{label (до 10), v, hi?}` — 3–7 |
| `statcard` | `elStatCard` | `label`, `value`, `spark[]` 5–9 чисел, `delta`, `note?` |
| `ring` | `elRing` | `title`, `value`, `legend[]{label, sub}` — 2–3 |
| `venn` | `elVenn` | `left`, `right`, `center` |

- [x] **Шаг 1. `elColumn` первым — он задаёт образец для остальных**

Масштаб столбцов считается от максимума переданных `v`, нормировать
не нужно — так записано в договоре. Ширина столбца выводится из числа
столбцов, а не берётся константой: при семи столбцах подписи наезжают.
Подписи ряда — через `ужатьНабор`, единый кегль на всю строку.

- [x] **Шаг 2. `npm run check` после каждого элемента**

- [x] **Шаг 3. Записи в `tools/data-check.js` с длинными подписями**

```js
column:   { title: 'Расходы по кварталам', bars: [
            { label: 'I кв', v: 42 }, { label: 'II кв', v: 78, hi: true },
            { label: 'III кв', v: 55 }, { label: 'IV кв', v: 91 }] },
statcard: { label: 'Обращений в месяц', value: '12 480',
            spark: [30, 42, 38, 55, 61, 58, 74], delta: '▲ 18%' },
ring:     { title: 'Исполнение бюджета', value: 68, legend: [
            { label: 'Освоено', sub: '₸8.2B' }, { label: 'Остаток', sub: '₸3.9B' }] },
venn:     { left: 'Данные ведомств', right: 'Данные бизнеса', center: 'Единый профиль' },
chapter:  { eyebrow: 'Глава 2', title: 'Межведомственное взаимодействие',
            lines: 'Как устроен обмен', meta: '12 слайдов' }
```

- [x] **Шаг 4. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Пять элементов с одним массивом принимают данные"
```

---

### Задача 4. Три элемента, где данные управляют положением

**Файлы:**
- Изменить: `src/elements.js` — `elBubble`, `elJourney`, `elDumb`
- Изменить: `tools/data-check.js`

| ид | функция | поля |
|---|---|---|
| `bub` | `elBubble` | `title`, `bubbles[]{label, x, y, r}` — 4–7 |
| `journey` | `elJourney` | `title`, `stages[]{label, mood}` — 4–6 |
| `dumb` | `elDumb` | `title`, `rows[]{label, a, b}` — 3–5 |

Отличие от задачи 3: подпись и координата берутся из одного объекта,
и подпись обязана ехать за фигурой. Именно здесь внешняя подстановка
текста давала бы шкалу в 78% с подписью «40%».

- [ ] **Шаг 1. `elDumb` — две точки и подпись между ними**

Проверить случай `a > b`: линия рисуется в обратную сторону, подпись
значения не должна наезжать на точку.

- [ ] **Шаг 2. `elBubble` — радиус из данных**

`r` нормировать от максимума; при семи пузырях проверить, что подписи
не перекрываются — проверка `разметка` это ловит, смотреть её вывод.

- [ ] **Шаг 3. `elJourney` — `mood` задаёт высоту точки**

Договор описывает `mood` **числом 1–5** — это высота точки на кривой,
а не название настроения. Проверить поведение при 0 и при 9: точка
не должна уходить за карточку.

- [ ] **Шаг 4. Записи в `data-check.js`**

```js
dumb:     { title: 'Срок обработки заявки', rows: [
            { label: 'Регистрация', a: 14, b: 3 }, { label: 'Согласование', a: 21, b: 8 },
            { label: 'Выдача результата', a: 9, b: 2 }] },
bub:      { title: 'Ведомства по нагрузке', bubbles: [
            { label: 'Минцифры', x: 30, y: 70, r: 22 }, { label: 'Минфин', x: 65, y: 45, r: 16 },
            { label: 'Минтруда', x: 45, y: 25, r: 12 }, { label: 'Минюст', x: 80, y: 80, r: 9 }] },
journey:  { title: 'Путь заявителя', stages: [
            { label: 'Узнал об услуге', mood: 'ровно' }, { label: 'Собрал документы', mood: 'плохо' },
            { label: 'Подал заявление', mood: 'ровно' }, { label: 'Получил результат', mood: 'хорошо' }] }
```

`mood` — значения брать из зашитого образца, новых не выдумывать.

- [x] **Шаг 5. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Три элемента с позиционированием по данным"
```

---

### Задача 5. Пять элементов со сложной геометрией

**Файлы:**
- Изменить: `src/elements.js` — `elWaterfall`, `elTree`, `elRadar`,
  `elPyramid`, `elVec`
- Изменить: `tools/data-check.js`

| ид | функция | поля |
|---|---|---|
| `wf` | `elWaterfall` | `title`, `steps[]{label, v, type}` — 3–7 |
| `tree` | `elTree` | `title`, `blocks[]{label, v}` — 4–6 |
| `radar` | `elRadar` | `title`, `axes[]{label, v}` — 5–7 |
| `pyramid` | `elPyramid` | `title`, `levels[]{label}` — 3–5 |
| `vec` | `elVec` | `title`, `clusters`, `note` |

- [ ] **Шаг 1. `elWaterfall` — накопление**

Каждый следующий столбец стоит на итоге предыдущего. `type` различает
приращение, потерю и итог; итоговый столбец считается из суммы, а не
берётся из данных — иначе можно передать несходящуюся картинку.
Если сумма не сходится, добавить замечание в предупреждения через договор.

- [ ] **Шаг 2. `elRadar` — число осей меняет всю сетку**

Углы считаются от числа осей: `2π i / n`. Подписи по кругу — самое
опасное место для проверки «зазор подпись↔фигура»: при семи осях
верхняя и нижняя подписи подходят к контуру ближе всего.

- [ ] **Шаг 3. `elTree` и `elPyramid` — площади и уровни**

У `tree` **раскладка фиксированная**: договор прямо говорит, что площади
заданы вёрсткой и от значений не пересчитываются, а `v` — строка вроде «38%»,
а не число. Подставляются только подписи и доли текстом.
У `pyramid` ширина уровня выводится из его номера, а не задаётся списком.

- [ ] **Шаг 4. `elVec` — `clusters` массивом из трёх**

Договор: массив, количество «ровно 3», по шесть точек в каждом,
координаты заданы вёрсткой. Подставляются подписи скоплений,
а не их положение.

- [ ] **Шаг 5. Записи в `data-check.js`**

```js
wf:       { title: 'От выручки к прибыли', steps: [
            { label: 'Выручка', v: 100, type: 'итог' }, { label: 'Себестоимость', v: -38, type: 'потеря' },
            { label: 'Операционные', v: -21, type: 'потеря' }, { label: 'Прочие доходы', v: 7, type: 'прирост' },
            { label: 'Прибыль', v: 48, type: 'итог' }] },
tree:     { title: 'Структура расходов', blocks: [
            { label: 'Персонал', v: '42%' }, { label: 'Инфраструктура', v: '27%' },
            { label: 'Лицензии', v: '18%' }, { label: 'Прочее', v: '13%' }] },
radar:    { title: 'Зрелость процессов', axes: [
            { label: 'Автоматизация', v: 78 }, { label: 'Прозрачность', v: 55 },
            { label: 'Скорость', v: 91 }, { label: 'Качество данных', v: 64 },
            { label: 'Безопасность', v: 83 }] },
pyramid:  { title: 'Уровни зрелости', levels: [
            { label: 'Стратегия и цели' }, { label: 'Процессы и регламенты' },
            { label: 'Данные и системы' }] },
vec:      { title: 'Векторное пространство',
            clusters: ['Договоры', 'Регламенты', 'Обращения'],
            note: 'запрос находит ближайшее по смыслу' }
```

У `wf` типы шагов — перечисление `t` / `p` / `n` (база или итог,
приращение, потеря) с ограничением: первый и последний шаг обязаны быть `t`.

- [x] **Шаг 6. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Пять элементов со сложной геометрией принимают данные"
```

---

### Задача 6. Три элемента с двумя массивами

**Файлы:**
- Изменить: `src/elements.js` — `elTree2` (`dtree`), `elSpark`, `elArea`
- Изменить: `tools/data-check.js`

| ид | функция | поля |
|---|---|---|
| `dtree` | `elTree2` | `title`, `root`, `branches[]{label, node, children}` — ровно 2 |
| `spark` | `elSpark` | `title`, `metrics[]{label, data, delta}` — 2–4 |
| `area` | `elArea` | `title`, `points`, `labels`, `delta?` |

- [ ] **Шаг 1. `elSpark` — массив внутри массива**

`data` каждой метрики — свой ряд чисел. Число метрик задаёт высоту строки,
длина ряда — шаг по горизонтали. Оба вложенных размера переменные,
это единственный такой случай в библиотеке.

- [ ] **Шаг 2. `elArea` — `labels` короче `points` по замыслу**

Договор прямо говорит: «подписываются не все точки, а 3–4 опорные».
Значит совпадение длин не требуется и требоваться не должно — подписи
раскладываются по опорным точкам, а не по каждой.

- [ ] **Шаг 3. `elTree2` — ровно две ветви**

Договор жёсткий: «ровно 2». Проверка количества уже стоит в
`сверитьДанные`, отдельного кода не требуется — убедиться, что сообщение
приходит, и не глушить его.

- [ ] **Шаг 4. Записи в `data-check.js`**

```js
spark:    { title: 'Ключевые метрики', metrics: [
            { label: 'Обращений', data: [30, 42, 38, 55, 61, 58, 74], delta: '▲ 18%' },
            { label: 'Среднее время', data: [70, 62, 58, 44, 39, 35, 28], delta: '▼ 41%' },
            { label: 'Повторных', data: [22, 19, 20, 15, 14, 12, 9], delta: '▼ 33%' }] },
area:     { title: 'Нагрузка на контур', points: [12, 28, 24, 41, 58, 52, 74],
            labels: ['Янв', 'Апр', 'Июл', 'Окт'], delta: '▲ 34%' },
dtree:    { title: 'Выбор способа подачи', root: 'Есть ЭЦП?', branches: [
            { label: 'да', node: 'Онлайн', children: ['Мгновенно', 'Без визита'] },
            { label: 'нет', node: 'ЦОН', children: ['Очередь', 'Визит лично'] }] }
```

- [x] **Шаг 5. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Три элемента с вложенными массивами принимают данные"
```

---

### Задача 7. `trans` — сперва договор, потом код

**Файлы:**
- Изменить: `spec/elements.json` — договор `trans`
- Изменить: `src/elements.js` — `elTrans`
- Изменить: `tools/data-check.js`

Сейчас в договоре написано прямым текстом: «элемент пока не параметризован —
данные зашиты, поскольку структура блока фиксирована определением
архитектуры». Пять рядов — Add & Norm, Feed Forward, Add & Norm,
Multi-Head Attention, Input Embedding — действительно заданы архитектурой
трансформера, менять их местами бессмысленно.

- [ ] **Шаг 1. Решить, что здесь вообще данные**

Параметризуемое: подпись повторов (`×12`), заголовок, возможно —
подписи рядов для перевода на русский. Структура — нет.
Записать это в договор явно, вместе с причиной.

- [ ] **Шаг 2. Перевести `elTrans` под получившийся договор**

- [ ] **Шаг 3. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "trans: договор о данных и параметризация"
```

---

### Задача 8. Пересмотр геометрии: рост холста и зазоры

Задача расширена 21 августа: к росту холста добавлен зазор подписи у `journey`.
Обе правки меняют вёрстку и потребуют одного общего решения по эталону,
поэтому идут вместе, а не по отдельности.

**Зазор у `journey`.** Значение `mood=1` законно по договору (границы 1–5),
но всегда даёт нарушение: подпись стоит в 3.4px от точки при норме 10.
Причина — две константы сетки, линия на 0.75 высоты и подписи на 0.83.
Сдвинуть их нельзя без потери побайтового совпадения, поэтому сейчас
это записано в договоре как известное свойство. Здесь — починить.

### Долг по геометрии, измеренный поимённо

В заметках проекта записано «61 замечание, в основном пограничное, разбирать
не обязательно». Замер 21 августа показал, что они не рассыпаны по библиотеке,
а собраны в нескольких местах:

```
зазор подпись↔фигура   35 пар у 5 элементов
   micro×30  gauge×2  cycle×1  bus×1  layers×1
нижний отступ           9 пар у 1 элемента
   divider×9
тёмный на поле         17 пар у 6 элементов
   wf×5  dumb×4  bars×3  spark×3  column×1  area×1
```

**Тридцать пар из тридцати пяти — это `micro`.** Три подписи «БД» стоят
в 9.7px от фигуры при норме 10, во всех десяти доступных стилях. Промах
в **три десятых пикселя**, повторённый тридцать раз. Это не «пограничное
замечание, которое можно не разбирать» — это одна правка, снимающая
половину всего долга библиотеки.

`divider` — вторая по величине куча: девять пар, все по нижнему отступу.

Оставшиеся семнадцать про тёмный текст на поле — свойство стилей, не вёрстки,
и чинятся иначе. Их сюда не берём.

### Правило роста холста

Из отчёта живой проверки: «Заявлено, что высота переменная, но во всех моих
вызовах, включая заведомо перегруженный, вернулось ровно 190 — вместо роста
холста подписи ужимались до 6.5px. Когда именно срабатывает рост, я так
и не понял».

`wrapSvgH` зовут **7 функций из 52** (было 6, растёт по мере перевода
элементов). Правило из `spec/rules.json` — сжать до пола, потом растить —
применено выборочно.

**Файлы:**
- Изменить: `src/elements.js` — все функции с переменным числом рядов
- Изменить: `spec/rules.json` — если правило требует уточнения
- Изменить: `tools/data-check.js` — проба на рост

- [ ] **Шаг 0. Забрать накопленные долги по геометрии**

Найдено при переводе элементов, отложено сюда:

- `journey` при `mood=1` — подпись в 3.4px от точки при норме 10, следствие
  констант сетки 0.75 и 0.83.
- `bub.x` и `bub.y` не зажаты вовсе, в отличие от радиуса: при `x=-50`
  или `y=300` пузырь уходит за карточку.
- `wf` при накоплении, ушедшем в минус глубже масштаба положительной части:
  столбец рисуется целиком за пределами холста. Проверено числом —
  `steps:[{v:10,type:'t'},{v:-50,type:'n'},{type:'t'}]` даёт
  `<rect x="110" y="-10" height="82">` при карточке от `y=14`. Договор
  знак и величину не ограничивает, так что вход достижим реальными
  данными: убыток крупнее накопленной базы.

**Общее у этих двух — не элементы, а проверки.** `проверить()` на обоих
случаях возвращает пустой список: содержимое **целиком за кромкой** не ловит
ни одна из шести. Они смотрят отступы, зазоры, контраст внутри карточки —
и молчат, когда смотреть уже не на что.

Значит нужна седьмая проверка либо расширение существующих: всё, что
нарисовано, обязано лежать в пределах холста. Это дешевле любой правки
отдельного элемента и закрывает целый класс — включая тот же пробел
у `elScatter`, откуда он унаследован.

- [ ] **Шаг 0.5. Снять половину долга одной правкой**

Начать с `micro`: подписи «БД» на 0.3px ближе нормы. Сдвинуть их — и 30 пар
из 35 уходят разом. Отрисовка образца при этом меняется, эталон придётся
пересобрать для десяти пар `<стиль>/micro` — порядок как в наряде «chip»:
`check` называет пары поимённо, разошлась чужая — остановиться.

Следом `divider` — девять пар по нижнему отступу.

Эти две правки стоит сделать и **измерить результат до остального**:
если после них останется 22 замечания вместо 61, дальнейший объём работы
выглядит иначе.

**Решение по эталону принято заранее** (ведущим, 21 августа): чинить, эталон
пересобрать для 19 пар — десять `micro` и девять `divider`. Владельца об этом
не спрашивали: два предыдущих раза, с `chip` и с `column`, он выбирал чинить,
и третий вопрос об одном и том же стоил бы больше, чем сама правка.

Отличие от тех случаев названо честно: там были дефекты поведения — выдуманное
число, плоские столбцы, — а здесь косметика в доли пикселя. Довод в пользу
починки: тридцать пар постоянных предупреждений приучают не читать
предупреждения вовсе. Библиотека, которая всегда ворчит, обесценивает
собственные проверки.

Отменяется откатом двух коммитов, пока ветка не слита.

- [ ] **Шаг 1. Перечислить, где рост нужен, а где нет**

Рост осмыслен там, где число рядов переменное. У `kpi`, `bignum`,
`divider` рядов нет — там рост не нужен, и это надо записать, чтобы
не искать заново.

- [ ] **Шаг 2. Проба: перегруженный элемент растёт, а не ужимается в 6.5px**

```js
проба('перегруженный элемент растит холст, а не кегль',
  результат['холст']['высота'] > 190 || минимальныйКегль(результат['svg']) >= 8);
```

- [ ] **Шаг 3. Довести до правила остальные**

Порядок для каждой функции: посчитать визуальный низ содержимого,
прибавить нижний отступ 16 и `PAD`, передать в `рост()`, заменить
`wrapSvg` на `wrapSvgH` и `frame` на `frameH`. При hh = 190 разметка
совпадает побайтово, поэтому `npm run check` обязан остаться чистым
на каждом шаге — это и есть признак, что замена сделана верно.

Пол кегля брать из `spec/rules.json`, а не из головы: правило гласит
сжимать до пола и только потом растить, а не наоборот.

- [x] **Шаг 4. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Правило роста холста применено во всех элементах с рядами"
```

---

### Задача 9. Шкала дельты в `retro` перестаёт врать

Найдено при переводе `kpi` на данные, 21 августа. Ветка `retro` в `chip()`
рисовала зашитое `'+23%'` вместо переданной дельты — это исправлено отдельным
нарядом, эталон пересобран для пары `retro/kpi`. **Но шкала осталась зашитой:**

```js
case 'retro':{ // дельта шкалой блоков вместо текста
  let o='';for(let i=0;i<5;i++)o+=`<rect ... fill="${i<4?s.pos:'#2a5c40'}"/>`;
  return o+txt(s,x+66,ty,label,12.5,700,s.pos,null);}
```

Четыре блока из пяти закрашены всегда, независимо от данных. Значит падение
на 41% соседствует со шкалой, показывающей рост почти по максимуму. Текст
теперь честный, картинка рядом — нет, и картинка убедительнее текста.

Причина в устройстве: `chip()` принимает только строку. Числа у неё нет,
и взять его неоткуда — `kpi.delta` по договору строка («▲ 23% г/г»),
потому что в неё входят и знак, и единицы, и период.

**Файлы:**
- Изменить: `src/engine.js` — сигнатура и ветка `retro` функции `chip()`
- Изменить: `src/elements.js` — единственный вызов `chip()`, в `elKpi`
- Изменить: `spec/elements.json` — договор `kpi`
- Изменить: `tools/smoke.js` — пробы
- Изменить: `tools/baseline.json` — только если образцовая отрисовка меняется намеренно

**Правило, которое тут проверяется:** элемент не рисует того, чего не знает.
Нет числа — нет шкалы. Пустое место честнее выдуманного заполнения.

- [ ] **Шаг 1. Проба, что шкала следует за данными**

```js
const мало = JSON.parse((await вызов('draw',
  { id: 'kpi', style: 'retro', data: { label: 'Отток', value: '3%', delta: '▼ 41%', delta_v: 0.1 } })).текст);
const много = JSON.parse((await вызов('draw',
  { id: 'kpi', style: 'retro', data: { label: 'Рост', value: '88%', delta: '▲ 41%', delta_v: 0.9 } })).текст);
const блоков = svg => (svg.match(/fill="#2a5c40"/g) || []).length;   // тусклые = незакрашенные
проба('шкала дельты следует за данными', блоков(мало) > блоков(много),
  `при 0.1 тусклых ${блоков(мало)}, при 0.9 — ${блоков(много)}`);
```

- [ ] **Шаг 2. Убедиться, что проба падает**

`npm run smoke` → СБОЙ: при любых данных тусклый блок ровно один.

- [ ] **Шаг 3. Договор `kpi` получает необязательное число**

```json
"delta_v": { "тип": "число", "границы": "0–1", "необязательно": true,
             "заметка": "доля шкалы в стиле retro; без неё шкала не рисуется" }
```

Имя поля латиницей, как все прочие в договорах. Число, а не процент:
дельта может быть и «×2.4», и «−41%», доля же всегда 0–1.

- [ ] **Шаг 4. `chip()` принимает долю**

```js
function chip(s,x,y,label,доля){
  ...
  case 'retro':{
    let o='';
    if(доля!=null){                                  // нет числа — нет шкалы
      const залито=Math.max(0,Math.min(5,Math.round(доля*5)));
      for(let i=0;i<5;i++)o+=`<rect x="${x+i*12}" y="${y+4}" width="9" height="17" fill="${i<залито?s.pos:'#2a5c40'}"/>`;
    }
    return o+txt(s,x+(доля!=null?66:13),ty,label,12.5,700,s.pos,null);}
```

Сдвиг подписи обязателен: без шкалы текст, начинающийся с 66, повиснет
в пустоте справа. Отступ 13 — тот же, что в остальных ветках `chip()`.

- [ ] **Шаг 5. `elKpi` передаёт долю**

```js
o+=chip(s,cx,base+92,ужать(delta,bч,12.5,12.5)[0], дан&&дан.delta_v);
```

Образец `ОБР_kpi` доли не содержит — значит без данных шкала не рисуется,
и отрисовка образца меняется. Это второе намеренное изменение эталона.

- [ ] **Шаг 6. Решить судьбу образца — до пересборки эталона**

Развилка, и её надо пройти осознанно: либо образец получает `delta_v: 0.8`
и картинка образца остаётся прежней (шкала есть, эталон не меняется вовсе),
либо образец доли не имеет и шкала из образцовой отрисовки исчезает.

Первое сохраняет эталон и показывает возможность элемента. Второе честнее:
образец перестаёт демонстрировать то, чего договор не требует. Выбрать
и записать выбор в отчёт с обоснованием.

- [ ] **Шаг 7. Эталон — по порядку из наряда «chip»**

`npm run check` обязан назвать поимённо каждую разошедшуюся пару. Ожидается
не более одной — `retro/kpi`, и только если на шаге 6 выбран второй путь.
Разошлось больше — остановиться и разобрать, а не пересобирать.

- [ ] **Шаг 8. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Шкала дельты в retro следует за данными, а не за константой"
```

---

### Задача 10. Поиск понимает человека, а не спецификацию

Замерено 21 августа на 25 запросах, сформулированных как их произносит человек,
а не как записано в назначении элемента:

```
запросов 25: первым 15, в тройке ещё 6, мимо 4
```

Четыре промаха и чем они обернулись:

| запрос | нужен | выдал |
|---|---|---|
| как клиенты отваливаются на каждом шаге | `funnel` | journey, kpi, wf |
| этапы проекта со сроками, часть пройдена | `roadmap` | timeline, cicd, journey |
| как менялась нагрузка за год | `area` | vec, faq, funnel |
| представить себя перед выступлением | `speaker` | thanks, agenda, divider |

Причина общая: поиск сравнивает основы слов, а человек говорит другими словами.
«Отваливаются» и «сужается» — про одно, общей основы нет.

**Файлы:**
- Изменить: `src/index.js` — функция `искать()` и её веса
- Изменить: `spec/elements.json` — поле `синонимы` у элементов
- Создать: `tools/find-check.js` — мерная проба, готовый набросок лежит
  в `.superpowers/sdd/PLAN/findcheck.mjs`
- Изменить: `package.json` — скрипт `find`

- [ ] **Шаг 1. Перенести мерную пробу в репозиторий**

Набросок с 25 запросами уже написан. Перенести в `tools/find-check.js`,
добавить скрипт `"find": "node tools/find-check.js"`, прогнать и убедиться,
что цифры воспроизводятся: **первым 15, в тройке ещё 6, мимо 4**.

Без этого шага улучшение измерить нечем, и «стало лучше» останется мнением.

- [ ] **Шаг 2. Починить ранжирование по `не_применять`**

Поле устроено как словарь «ситуация → название лучшего элемента»:

```json
"cicd": { "этапы не про поставку": "стрелки-шаги", "нужны сроки": "дорожная карта" }
```

Таких переадресаций **144 на 52 элемента**, и **124 из них указывают на точное
название существующего элемента**. Это готовая карта «хочешь вот это — бери вон то».

Сейчас поиск использует её наоборот: совпадение с ключом добавляет балл
**самому элементу**. Поэтому на «сроки» вылезает `cicd` — у него в
противопоказаниях написано «нужны сроки → дорожная карта», и он поднимается
вместо самой дорожной карты.

Надо: совпало с ключом — поднимать элемент, **названный в значении**.

```js
/* Указатель «не я, а он» должен вести к нему, а не тянуть балл на себя */
const поИмени = new Map([...ЭЛЕМЕНТЫ.values()].map(э => [э.название.toLowerCase(), э.ид]));
```

Оставшиеся 20 переадресаций записаны прозой и на точное имя не ложатся:
«treemap или кольцевая», «таймлайн или дорожная карта», «разбить на два слайда».
Для них: искать имя элемента внутри строки, а если ни одного нет — совет
адресован человеку, а не поиску, и балла не даёт никому.

- [ ] **Шаг 3. Поле `синонимы` в договорах**

Слова живой речи, которых нет в назначении. По 3–6 на элемент:

```json
"funnel": { "синонимы": ["воронка продаж", "конверсия", "отвал", "отваливаются", "сужается поток"] },
"roadmap": { "синонимы": ["сроки", "план-график", "когда что будет", "вехи проекта"] },
"area":    { "синонимы": ["динамика", "за год", "нагрузка во времени", "тренд"] },
"speaker": { "синонимы": ["о себе", "представиться", "кто выступает", "спикер"] }
```

Заполнять **по промахам, а не подряд**: начать с четырёх провалившихся,
прогнать пробу, добавить следующим по счёту непопаданиям. Синоним, который
ничего не чинит, — лишний вес в поиске.

Вес синонимов в `искать()` ставить не выше веса назначения: синоним помогает
найти, но не должен перебивать смысл.

- [ ] **Шаг 4. Довести до цели и не сломать остальное**

Цель: **первым не меньше 21 из 25, мимо ноль.** Прогонять пробу после каждой
правки — улучшив один запрос, легко испортить три.

- [ ] **Шаг 5. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host && npm run find
git add -A && git commit -m "Поиск: переадресация из не_применять и синонимы живой речи"
```

---

### Задача 11. Проверить путь Claude Design, а не только путь Claude Code

В постановке сказано: сервер возвращает **либо готовый SVG, либо его полное
описание для Claude Design**. Это два разных пути, и проверены они неодинаково.

Путь `draw` — отдать готовую разметку — проверен многократно: четыре агента
с чистым контекстом, `data-check` на 43 элементах, эталон 676 пар.

Путь `describe` — отдать числа, чтобы модель нарисовала сама, — **не проверен
ни разу**. Композиция в долях холста появилась только 21 августа, и до сих
пор никто не пробовал по ней рисовать. Мы знаем, что числа верны (измерены
и перепроверены), но не знаем, достаточны ли они.

**Файлы:**
- Создать: `tools/design-check.js` — прогон и сличение
- Изменить: `spec/elements.json` — то, чего не хватит, дописывается в композицию

- [ ] **Шаг 0.1. Выяснить, что Claude Design вообще принимает**

Никем не проверено, и на этой машине проверить нечем: Design не установлен,
весь прогон 21 августа шёл в Claude Code.

Неизвестно главное: принимает ли Design **готовую разметку SVG** в колоду.
От этого зависит, какой путь основной, а какой запасной. В `ГДЕ-ЧТО.md`
есть строка «Claude Design просит живые образцы, а не только спецификации» —
но она из исходных документов проекта, а не из проверки.

Проверять нужно в самом Design: подключить сервер, попросить слайд,
посмотреть, что он делает с ответом `draw` и что — с ответом `describe`.
Пока это не сделано, любые рассуждения о «главном пути» — догадки.

- [ ] **Шаг 0. Сперва перемерить сами числа**

Композиция описана числами у всех 52 элементов (задача 1а), но проверена
выборочно: проверяющий сверил 14 элементов и нашёл одну ошибку — у `cycle`
расстояние подписи от центра было 0.22 вместо 0.15.

**Пятнадцатая сверка нашла вторую.** У `pyramid` записано «ширина_низа
0.57 ширины», настоящее основание — 213px, то есть 0.71. Расхождение
в семь раз больше порога, с которым эти числа принимали.

Две ошибки на пятнадцать проверенных — это не единичный промах. Прежде
чем проверять достаточность описания, надо убедиться в его верности:
рисующий по неверному числу сообщит нам о дыре, которой нет.

Мерить так же, как мерили в задаче 1а: отрисовать элемент, снять координаты,
сверить с записанным, порог 0.02. Начать с тех, кого в выборку 1а не брали.

- [ ] **Шаг 1. Взять элемент и отдать только описание**

Агенту с чистым контекстом даётся вывод `describe` для элемента — и запрет
вызывать `draw` и заглядывать в `src/`. Задача: нарисовать SVG 300×190
по этому описанию.

Начать с трёх разной сложности: `kpi` (карточка, три текста),
`bars` (ряды с шкалой), `radar` (радиальная сетка).

- [ ] **Шаг 2. Сличить не побайтово, а по смыслу**

Совпадения байт в байт быть не может и не нужно. Сличать надо то, ради чего
композиция и писалась:

- попали ли ключевые элементы в свои доли холста (±0.02);
- соблюдены ли инварианты — нижний отступ 16, зазор подписи 10;
- узнаётся ли элемент: столбцы столбцами, кольцо кольцом.

Прогнать результат через `проверить()` из `src/checks.js` — те же шесть
проверок, что и для наших отрисовок. Если чужая отрисовка их проходит,
описание достаточно.

- [ ] **Шаг 3. Дописать то, чего не хватило**

Каждое место, где рисующий вынужден был догадываться, — дыра в композиции.
Записать её числом, а не словами. Это и есть результат задачи.

- [x] **Шаг 4. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Композиция проверена самостоятельной отрисовкой"
```

---

### Задача 16. Мера текста: регистр и таблица ширин

Из разведки по чужим библиотекам, 22 августа. Проверено сверкой с реальными
метриками Arial и Segoe UI как прокси к Inter.

**Наш коэффициент 0.55 точнее, чем казалось, но врёт не там, где думали.**

```
'Выручка'                оценка/реальность = 0.97
'Ключевые показатели'    = 1.01
'Инвестиции в развитие'  = 1.06     ← обычные подписи: ±7%, в безопасную сторону

'ВЫРУЧКА'                = 0.81     ← недобор 19%
'КПЭ'                    = 0.82
'ЕБИТДА'                 = 0.83
'III кв. 2026'           = 1.36     ← перебор 36%
```

**Недобор на прописных системен: семь стилей из тринадцати поднимают регистр** —
`neon`, `duotone`, `retro`, `aurora`, `line`, `brutal`, `memphis`. Это 54%
библиотеки, и там каждая подпись меряется с недобором в пятую часть.

**Важно, чего это НЕ объясняет.** Двукратный разрыв в объявленных пределах
коэффициентом не объясняется — класс ошибки здесь ~20%, и только на прописных
и на строках из узких либо широких знаков. Двойка пришла из задачи 15,
из выброшенного кегля. Эти две правки независимы и складываются.

**Отраслевой контекст, чтобы не переоценить задачу.** Vega без canvas считает
`0.8 × длина × кегль` — грубее нас. zrender (движок ECharts) держит таблицу
на 95 символов ASCII, а любой кириллической букве присваивает ширину, равную
кеглю, то есть коэффициент 1.0 — вдвое хуже нас. Строго меряют только те,
кто читает файл шрифта: Satori, python-pptx через PIL.

### Замерено 22 августа, до начала работ

**Правка меры стоит 102 пары эталона из 676 — 15% библиотеки, 12 элементов.**

Инструментировал `ужать`, прогнал все 676 пар с обоими коэффициентами и считал,
где решение «влезает / не влезает» меняется:

```
пар всего 676 | задето 102 (15%)
из них в стилях с капсом 84 | в остальных 18
элементов затронуто 12 из 52
```

Восемнадцать пар вне капса — потому что **заглавная буква в начале слова тоже
считается прописной**: «Лицензии» при кегле 9 даёт 39.6 по старой мере и 41.2
по новой при пределе 40. Одной буквы хватает, чтобы перевалить.

**Что это значит на самом деле.** Если новая мера вернее, то эти 102 пары
**уже сейчас рисуются с текстом шире отведённого** — мы просто не знали.
Правка не портит образцы, а показывает правду. Но пересборка эталона на 15%
библиотеки — решение владельца, а не исполнителя.

### Чем мерить: шрифта Inter нет ни в системе, ни в репозитории

Проверено: `C:/Windows/Fonts` содержит `arial.ttf`, `segoeui.ttf`, `tahoma.ttf`;
файлов Inter — ноль. В репозитории шрифтов нет вовсе.

Наша разметка объявляет `font-family="Inter,system-ui,sans-serif"`. Inter —
веб-шрифт, в Windows и macOS его штатно нет. **Значит почти везде рисуется
запасной: на Windows это Segoe UI, на macOS — SF Pro.**

Отсюда переворот в постановке задачи: мерить надо **не Inter**, а то, чем
на самом деле нарисуется. И раз это зависит от машины зрителя и мы её не знаем,
мера должна быть **осторожной** — завышать ширину, чтобы текст ужимался
раньше, а не вылезал.

Замеры разведки как раз по запасным шрифтам:

```
                        Arial   Segoe UI
строчная кириллица      0.578     0.573
взвешенная по частоте   0.548     0.544
ПРОПИСНАЯ               0.726     0.684
цифры                   0.556     0.539
```

Наш нынешний 0.55 хорошо откалиброван под строчную обоих. Для прописной
осторожный выбор — **0.73**, больший из двух: завышение безопасно, занижение нет.

### Порядок шагов изменён

Изначально было «дешёвый шаг 1, потом точный шаг 2». Замер показал, что шаг 1
не дешёвый — он стоит те же 102 пары. Значит платить надо **один раз и сразу
за верные числа**, а не дважды за приблизительные.

- [ ] **Шаг 1. Промежуточный шаг: четыре коэффициента вместо одного**

Снимает 80% боли, не трогая ни архитектуры, ни зависимостей:

```
строчная кириллица  0.55      прописная кириллица  0.73
цифры               0.556     пробел               0.28
```

Уничтожает единственный найденный системный недобор — заголовки капсом.

**Осторожно с эталоном.** Мера участвует в `ужать`, а `ужать` на образцах
ни разу не срабатывает (676 пар, ноль сжатий — замерено). Значит правка меры
образцы менять не должна. Разошлось — разобрать и доложить.

- [ ] **Шаг 2. Таблица ширин на символ**

Дизайн — как у zrender `DEFAULT_TEXT_WIDTH_MAP` и `js-server-text-width`:
снять advance widths из TTF Inter для набора, который мы вообще выводим —
ASCII, кириллица, `₽ № « » —`, около трёхсот записей. Сложить в `spec/`,
подключить `src/metrics.js`, сигнатуру `мера(str, fs)` не менять.

**Зависимостей не добавлять.** У нас в `package.json` кроме MCP SDK ничего нет,
и это стоит сохранить: таблица снимается один раз при её сборке, в рантайме —
сложение чисел.

Чего **не** делать: нативный canvas (`@napi-rs/canvas`, `skia-canvas`) —
платформенный бинарник в MCP-сервере несоразмерен выигрышу над таблицей.
`harfbuzzjs` — для кириллицы избыточен. Всё, что через DOM, — mermaid до сих
пор тащит Puppeteer именно потому, что не смог уйти от `getBBox`.

---

### Задача 17. Политика автофита записана числами, а не подразумевается

Из той же разведки. Главный вывод по отрасли: **ни одна библиотека не выбирает
между «растить» и «ужимать» по данным в момент отрисовки.** Режим задаёт автор
шаблона заранее, флагом на фигуре. Автоматика живёт только внутри выбранного
режима.

У нас режим не задан нигде. Пять механизмов адаптации применяются вразнобой —
это прямо отмечено в живом прогоне: «`bars` с пятью рядами растит холст,
а `llm` с пятью моделями — нет».

- [ ] **Шаг 1. Режим на текстовый слот, тройкой OOXML**

В `spec/elements.json` каждому текстовому слоту — один из трёх:

```
noAutofit    геометрия жёсткая, текст обязан влезть (текущее умолчание)
normAutofit  ужимаем текст под слот
spAutoFit    растим блок под текст
```

**Наше отличие от всех:** `spAutoFit` доступен только внутри блока и никогда
на уровне холста — 300×190 не двигается. Именно поэтому политики роста
Graphviz, Excalidraw и Vega-Lite к нам не переносятся: все трое исходят
из резинового холста.

- [ ] **Шаг 2. Лестница вместо непрерывного подбора**

LibreOffice реверсил алгоритм MSO ради совместимости — коммит `f61ea13`,
`constScaleLevels`: **тринадцать фиксированных ступеней**, от `{100, 100}`
до `{25, 25}`, применяются по порядку, пока не влезет.

Дискретность здесь не эстетика, а требование: непрерывный подбор делает каждый
из 676 слепков чувствительным к изменению метрики на десятую долю процента,
лестница из четырёх ступеней — нет. Для нас это прямо связано с задачей 16:
уточнение меры не должно ломать эталон.

- [ ] **Шаг 3. Пол кегля — числом**

Пол существует ровно у двоих: MSO — 25% от исходного, tldraw — 14px абсолютных.
У остальных пола нет, потому что нет и ужатия.

Наши 25% нереальны: при подписях 8–13px на холсте 300×190 ниже примерно 85%
подпись перестаёт читаться в слайдовом масштабе. Наш пол — вопрос к владельцу,
но он **должен быть записан числом**, а не подразумеваться.

- [ ] **Шаг 4. Ниже пола — сменить стратегию, а не ужимать дальше**

Политика tldraw, `measureNoteLabelSize`: ужимать по 1px не более 50 раз,
и явный порог `if (fontSizeAdjustment <= 14) break` — ниже перестаёт ужимать
и передаёт дело переносу по словам. Перенос у нас уже есть в `engine.js`,
не хватает пола и точки переключения.

- [ ] **Шаг 5. Переменное число рядов: отбрасывать хвост, а не сжимать всех**

Vega формулирует политику прямо (`symbolLimit`): «если записей больше предела,
лишние отбрасываются и заменяются многоточием». Это лучше сжатия шага: сжатие
ухудшает **все** ряды, отбрасывание — только хвост.

Порог брать геометрически, а не счётчиком: «измеренный габарит больше
доступного». Счётчиковые пороги в отрасли нашлись всего два.

---

### Задача 12. Элементу нечем сказать, что с данными беда

Найдено при переводе `wf`, 21 августа. Договор требует, чтобы первый и последний
шаг водопада были типа `t` — база и итог. Итоговый столбец **вычисляется**
как накопление предыдущих, а не берётся из данных: иначе можно передать
несходящуюся картинку и получить график, безупречный геометрически и ложный
арифметически.

Но если переданный итог не сошёлся с накопленным, пользователь об этом
не узнаёт. Молча рисуется накопленное.

**Дыра не в элементе, а в устройстве.** Сегодня о данных могут сказать только двое:

- `сверитьДанные` в `src/index.js` — сверяет состав полей, грубый тип,
  количество и длину строк. Про смысл значений не знает ничего.
- шесть проверок в `src/checks.js` — смотрят готовую разметку: отступы,
  зазоры, контраст. Про исходные данные не знают ничего.

Между ними — пустота. Элемент **видит** беду в данных: сумма не сходится,
накопление ушло в минус, доли не дают ста процентов, «стало» больше «было»
в графике падения. Сказать об этом ему нечем.

**Чья задача.** Модели-сборщика: она передала данные и обязана узнать,
что с ними не так, — иначе перескажет человеку «готово» вместо «сумма
не сходится». И зрителя слайда: именно он увидит правдоподобную ложь.

**Файлы:**
- Изменить: `src/elements.js` — способ вернуть замечание из функции отрисовки
- Изменить: `src/index.js` — сбор замечаний элемента в общий список
- Изменить: `tools/smoke.js` — пробы

- [ ] **Шаг 1. Проба на несходящейся сумме**

```js
const кривой = JSON.parse((await вызов('draw', { id: 'wf', data: { title: 'Итог', steps: [
  { label: 'База', v: 100, type: 't' }, { label: 'Рост', v: 20, type: 'p' },
  { label: 'Итог', v: 999, type: 't' }] } })).текст);
проба('несходящийся итог замечен', кривой['предупреждения'].some(п => /итог/i.test(п)),
  кривой['предупреждения'].join(' | '));
```

Сначала убедиться, что падает: сейчас 999 молча заменяется на 120.

- [ ] **Шаг 2. Выбрать способ, которым элемент возвращает замечание**

Развилка, и её надо пройти осознанно, а не выбрать первое пришедшее.
Функция отрисовки сейчас возвращает строку разметки. Варианты:

- второй возвращаемый элемент — ломает сорок восемь вызовов;
- собирающий массив, переданный третьим аргументом, — не ломает никого,
  но добавляет параметр;
- поле на самой функции — не работает при параллельных вызовах,
  а мы уже видели, что параллельные вызовы бывают.

Выбрать, обосновать в отчёте, **не менять сигнатуру ради красоты**:
побайтовая совместимость 676 пар держится на том, что отрисовка без данных
не меняется.

- [ ] **Шаг 2.5. Отчёт о переполнении — машиночитаемый**

Из разведки, 22 августа: **структурного поля «не влезло» нет ни у кого.**
Потолок отрасли — строчка в stderr у Graphviz при `fixedsize=true`; все
остальные молча режут, молча растят или молча дают тексту вылезти.

Мы можем лучше, и по причине, которой у них нет: **наш потребитель — модель,
а не человек.** Ей строчка в stderr бесполезна, а машиночитаемое указание —
прямая инструкция сократить текст и перерисовать.

Вместо нынешнего «подписи не поместились и обрезаны: Первый конта…» —

```json
{ "слот": "rows[3].label", "текст": "Общая численность штата",
  "требуется": 96.4, "доступно": 78.0, "сделано": "обрезано" }
```

Поле аддитивно: SVG не меняется, все 676 слепков остаются валидными.

- [ ] **Шаг 3. Собрать известные случаи**

Из отчётов по нарядам 3–5:

- `wf` — итог не сходится с накоплением;
- `wf` — накопление ушло в минус, столбец рисуется не по ту сторону оси
  (предсуществующий дефект, воспроизводится данными из отчёта наряда 5);
- места, где значение молча зажимается: `radar.v`, `dumb.a/b`, `ring.value`,
  `journey.mood`, `bub.r`. Зажим — не ошибка, но пользователь должен узнать,
  что его 120 стало 100.

- [ ] **Шаг 4. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Элемент сообщает о беде в данных, а не молчит"
```

---

### Задача 13. Сплошная сверка композиции: все числа, а не выборка

**Почему понадобилась.** Владелец подтвердил 21 августа: способность нарисовать
элемент по описанию нужна и остаётся полноценным путём, а не запасным.

Значит числа композиции — не справка, а исходные данные для отрисовки.
А в них ошибки, и не единичные:

```
задача 1а   проверено 14 элементов выборочно   →  1 ошибка (cycle 0.22 против 0.15)
наряд  5    проверено 38 чисел у 5 элементов   →  2 ошибки (pyramid 0.57→0.71, tree 0.95→0.88)
```

Около **5% чисел неверны**. При среднем в 7–8 чисел на элемент это порядка
двадцати ошибок из четырёхсот по всей библиотеке.

**Ошибки не арифметические, а именные.** Оба расхождения наряда 5 — перепутанное
ребро: измерили одну границу, назвали другой. `0.57` у пирамиды было настоящей
величиной — верхней кромкой последнего яруса, но записано как «ширина низа».
Такое не ловится ни проверкой типов, ни здравым смыслом читающего. Только
измерением каждого числа по отдельности.

**Чья задача.** Модели-сборщика: она рисует по этим числам. И зрителя: пирамида
на треть уже задуманной — это его слайд.

**Файлы:**
- Создать: `tools/grid-check.js` — сверка чисел композиции с отрисовкой
- Изменить: `spec/elements.json` — исправленные числа

- [ ] **Шаг 1. Сверка вместо ручного пересчёта**

Числа в `сетка` записаны прозой: «0.11–0.85 ширины», «первая_строка 0.23 высоты»,
«4 равных яруса шагом 0.14». Разобрать их автоматически до конца не выйдет —
имена подполей у каждого элемента свои, общего словаря нет и не должно быть.

Поэтому сверка полуавтоматическая: скрипт достаёт из отрисовки все характерные
координаты — крайние точки, границы прямоугольников, центры окружностей,
базовые линии текста — и печатает их долями рядом с записанным текстом сетки.
Сверяет человек или агент, но не считает вручную.

- [ ] **Шаг 2. Пройти 47 оставшихся элементов**

Пятеро из наряда 5 уже сверены, `cycle` исправлен в задаче 1а. Остальные —
подряд, по разделам каталога. Каждое расхождение больше 0.02 — исправить,
и **записать, чем оказалось прежнее число**: как с пирамидой, где 0.57
была настоящей величиной не того ребра. Это отличает опечатку от подмены
понятия, а второе повторяется.

- [ ] **Шаг 3. Назвать итоговую частоту**

Сколько чисел проверено, сколько исправлено, какая доля. Если ошибок
окажется заметно больше пяти процентов — значит способ, которым числа
снимали в задаче 1а, ненадёжен сам по себе, и это отдельный вывод.

- [ ] **Шаг 4. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
git add -A && git commit -m "Композиция сверена сплошь: <N> чисел, <M> исправлено"
```

`check` обязан остаться «разошлись 0» — правится только спецификация.

---

### Задача 15. Ужатый кегль вычисляется и выбрасывается

> **Закрыта 22 августа, проверка независимая.** Шесть коммитов `0c14017..74530c4`.
> Замер до и после: вылезали за холст на своём же пределе **73 поля из 90 → 0**.
> Исполнитель нашёл четвёртый класс, которого не было в замере ведущего:
> `ужатьНабор(...)[0]` тоже отбрасывала кегль — `elHeat` дважды, `elTimeline`,
> `elFMatrix`. Проверка перемерила 50 полей, шесть элементов с рядами на предмет
> «кегль ужался, а место осталось», и намерила **фактический пол ужатия — 6**,
> у `elLayers` и `elBus`. Пол нигде не записан как решение — это задача 17.
> Мелочь на будущее: `chapter.eyebrow/title/meta` можно поправить 62→63, 29→30,
> расхождение в безопасную сторону.

**Найдено 22 августа измерением. Это корень задачи 14 и, вероятно, половины
находок живого прогона.**

`ужать(строка, maxW, кегль, пол)` подбирает кегль, при котором текст влезает
в ширину, и возвращает **пару**: подогнанный текст и подобранный кегль.

Сто вызовов из ста двенадцати берут только текст:

```js
o += txt(s, cx, base+22, t(s, ужать(label, bw, 13)[0]), 13, ...);
//                            подобранный кегль ─┘ выброшен  └─ рисуем исходным
```

Замер по `src/elements.js`:

```
ужать(...)[0] — кегль отброшен    100
ужать(...)[1] — кегль взят          1
через деструктуризацию             11
```

**Что из этого следует.** `ужать` считает: «чтобы 46 знаков влезли в 228px,
нужен кегль 9». Возвращает текст целиком и кегль 9. Вызывающий рисует его
кеглем 13 — и строка выходит 329px вместо 228, вылезая за холст на 65px.

Проверено на `kpi.label`: правый край текста **365 при холсте 300**.
Предупреждений ноль — с точки зрения `ужать` всё влезло.

**Отсюда же неверные пределы.** Вчерашние лимиты измерялись «подавать строки
растущей длины, пока не появится „обрезаны“». Обрезка наступает, когда `ужать`
упирается в пол кегля — то есть предел меряли **при уменьшенном кегле**,
а рисуют полным. Отношение пола к исходному — 0.72, отсюда завышение примерно
в 1.4 раза, что и совпадает с наблюдением живого прогона «завышены в полтора-два».

Замер: **73 поля из 90 с объявленной длиной вылезают за холст на своём же
объявленном пределе.**

**Починка ничего не стоит.** Инструментировал `ужать` и прогнал все 676 пар:

```
пар отрисовано: 676 | срабатываний со сжатием или обрезкой: 0
```

На образцах текст всегда влезает исходным кеглем, значит `ужать` возвращает
кегль неизменным, и подстановка возвращаемого значения **не меняет ни байта
образцовой отрисовки**. Эталон не пересобирается.

**Чья задача.** Зрителя слайда: текст уезжает за край. И модели-сборщика:
она читает лимиты из договора и верит им.

**Файлы:**
- Изменить: `src/elements.js` — сто вызовов
- Изменить: `spec/elements.json` — пределы после перемера
- Изменить: `tools/smoke.js` — пробы

- [x] **Шаг 1. Проба, которая сейчас падает**

```js
const длинный = JSON.parse((await вызов('draw',
  { id: 'kpi', data: { label: 'Ш'.repeat(46), value: '12', delta: '▲ 5%' } })).текст);
const узел = длинный['svg'].match(/<text([^>]*)>(Ш+)</);
const кегль = +узел[1].match(/font-size="([\d.]+)"/)[1];
const x = +узел[1].match(/ x="([\d.]+)"/)[1];
проба('подпись на своём пределе не выходит за холст',
  x + узел[2].length * кегль * 0.55 <= 300,
  `правый край ${Math.round(x + узел[2].length * кегль * 0.55)}`);
```

- [ ] **Шаг 2. Провести кегль до отрисовки**

Механическая правка ста мест: брать пару целиком и передавать в `txt()`
подобранный кегль вместо исходного.

```js
const [подпись, кг] = ужать(label, bw, 13);
o += txt(s, cx, base+22, t(s, подпись), кг, ...);
```

**`npm run check` обязан остаться «разошлись 0» после каждой партии.**
Разошлось — значит на этом образце `ужать` всё-таки ужимала, и случай надо
разобрать отдельно, а не узаконивать пересборкой.

- [ ] **Шаг 3. Перемерить все 90 пределов заново**

Прежние числа получены методом, который сам был испорчен. Мерить надо
не «когда появится „обрезаны“», а **когда правый край текста выйдет
за панель** — то есть по фактической геометрии отрисованного узла.

- [ ] **Шаг 4. Проверки и коммит**

```bash
npm run check && npm run data && npm run smoke && npm run host
```

---

### Задача 14. Разобрать живой прогон библиотеки своими данными

21 августа проведён прогон 39 элементов из 52 настоящими русскими данными
в стиле `glass`, двумя наборами на элемент: по договору и с отклонением
на границе. Отчёт — `data-contract-test-report.md` в корне репозитория.

**Главный результат прогона: все 37 элементов, прогнанных парами, приняли
данные без единой ошибки формата.** Договор описывает интерфейс точно.
Сломалось другое — вёрстка на границах.

**Часть находок уже закрыта** нарядом 8 и нарядом «шум», проверено измерением
21 августа вечером:

| из отчёта | состояние |
|---|---|
| `roadmap`, `swim`, `llm`, `fmatrix` не растят холст | **закрыто**, наряд 8 часть 4: `roadmap` теперь 210px вместо 190 |
| `$заметка` у `tree` даёт ложное предупреждение | **закрыто** нарядом «шум» |
| `scat` рисует за холстом молча | **ловится** седьмой проверкой: «за кромкой: circle габарит [321.5, 91.5, 330.5, 100.5] при холсте 300×190». Зажатия по-прежнему нет |

Остальное открыто. Ниже — по убыванию важности, как расставил сам отчёт.

**Чья задача.** Зрителя слайда: шесть элементов молча вылезают за панель.
И модели-сборщика: она читает лимиты из договора и верит им.

**Задача 15 закрыта 22 августа, и она сняла больше, чем ожидалось.**
Замер до и после:

```
полей с объявленной длиной                              90
вылезают за холст на своём же пределе:  было 73  →  стало 0
```

Самый яркий случай отчёта — `bignum.eyebrow` на 63 знаках — рисовался строкой
около 466px на холсте 300, вылезая на 83px с каждой стороны. Теперь ужимается
до кегля 6.5 и укладывается в 39…261.

**Пределы оказались верны с самого начала.** Перемер после починки: 87 из 90
совпали с объявленным дословно, три — на знак щедрее, ни одного опаснее.
То есть числа в договоре всегда описывали «сколько знаков вообще можно
показать», а неверной была отрисовка, которая их игнорировала.

Отсюда правка задачи: **шаг 2 (пересчитать завышенные лимиты) отменяется** —
пересчитывать нечего. Шаг 1 остаётся, но меняет смысл: проверка ширины
центрированного текста нужна теперь не как измеритель, а как **страховка**,
чтобы дефект такого класса не вернулся незамеченным.

Появился новый вопрос, которого не было: ужатие теперь доходит до кегля 6.5,
а **пол кегля у нас нигде не записан числом** — это задача 17.

**Причина найдена 22 августа — задача 15.** Ужатый кегль вычисляется
и выбрасывается в ста вызовах из ста двенадцати: `ужать` подбирает кегль,
при котором текст влезает, а рисуют исходным. Шаги 1 и 2 этой задачи делать
**после** задачи 15 — она снимает причину, а не следствие, и перемер пределов
после неё даст другие числа.

- [ ] **Шаг 1. Ширина центрированного текста не проверяется вовсе**

Самая крупная находка живого прогона и корень трёх других. Проверка на обрезку меряет ширину
только там, где текст выравнивается по левому или правому краю. У центрированного
не меряется ни на обрезку, ни на выход за кромку.

Отсюда `bignum.eyebrow`: договор обещает 64 знака, реально помещается около 36.
Переданные 63 знака рисуются целиком строкой шириной около 466px на холсте 300 —
вылезает за оба края примерно на 83px. **Предупреждений ноль.**

Проверено вечером: замечание не появилось и после наряда 8.

- [x] **Шаг 2. Пересчитать завышенные лимиты — ОТМЕНЁН**

Отменён 22 августа: после задачи 15 пересчитывать нечего, пределы верны.
Перемер дал 87 совпадений из 90, три расхождения в безопасную сторону.
Ниже — прежний текст шага, оставлен для истории.

<details>
<summary>прежний текст шага 2</summary>

Отчёт намерил семь мест, где договор завышен в полтора-два раза:

```
bignum.eyebrow   64 → ~36      gauge.caption   60 → ~48
bignum.caption   56 → ~46      venn.left/right 35 → ~14
bignum.delta     64 → ~50      swot.letter     13 → ~5
pyramid.levels[].label 46 → зависит от яруса
```

Все — центрированный текст. Пересчёт имеет смысл делать **после** шага 1:
измеритель, который их поймает, и даст верные числа.

</details>

- [ ] **Шаг 3. Зажать то, что осталось незажатым**

`scat.x/y` — договор объявляет 0–100, зажатия нет. Седьмая проверка теперь
об этом говорит, но лучше не допускать.

`heat.matrix` — значение 12 при объявленных 1–9 не зажалось, а **пересчитало
шкалу всей матрицы**: остальные ячейки поблекли. Одна ошибочная ячейка портит
чтение всей карты.

- [ ] **Шаг 4. `kpi.delta` — лимита нет, а обрезка есть**

Плашка фиксированной ширины 98px режет на десятом знаке. Собственный пример
библиотеки — «▲ 23% г/г», ровно 9 знаков — стоит на самой грани. Либо дать
лимит в договоре, либо растянуть плашку по содержимому.

- [ ] **Шаг 5. Мелкие расхождения описания и поведения**

`roadmap.cols`: `describe` пишет «до 6», валидатор требует «ровно 6» —
**пример из документации не проходит собственную валидацию**.

`bars`: предупреждение «холст вырос: подписи перенесены на несколько строк»
не соответствует разметке — переноса нет, упал кегль. Автор пойдёт искать
двухстрочные подписи, которых не существует.

`heat` при превышении лимита подписи строк: «нед1»…«нед4» превращаются
в четыре одинаковых «не…». Обрезка уничтожает различие — лучше ронять кегль.

- [ ] **Шаг 6. Палитра из шести позиций против договоров на семь**

`bub` на 7 пузырей, `arrows` на 5 шагов, `timeline`, `llm`, `roadmap`,
`pyramid` — последний элемент получает серый `rgba(255,255,255,.42)`.
На слайде читается как «неактивный» или «прочее», хотя в данных ничего
такого нет.

- [ ] **Шаг 7. Дожать непрогнанные 13 элементов**

`trans facts statcard ring icons quote before divider chapter thanks agenda
speaker faq` — два самых текстовых раздела библиотеки. Прогон остановили
сознательно: к сороковому элементу классы поведения повторялись. Но находка
по `bignum` даёт основание ждать там того же.

Проверить прежде всего `quote`, `facts`, `chapter`, `divider`, `speaker` —
там крупный центрированный текст.

---

## Что вынесено в отдельный план

**Переезд на HTTP снят с плана 21 августа** — по решению владельца, после
разбора действующих лиц. Причина: у этой работы нет ни человека, ни зрителя
слайда. Она нужна там, где к одному серверу обращаются параллельно; в связке
«один человек, один Клод, один локальный процесс» такого не бывает.

Разделяемая ячейка стиля при этом никуда не делась: у stdio нет `sessionId`,
ключ сеанса — константа, и четыре параллельных агента 21 августа переписали
стиль друг другу. Практический случай закрыт поштучным `style` в `draw`:
передал явно — нарисуется им, состояние сессии не трогается.

Вернуться к этому придётся, если появится второй одновременный потребитель:
облачный хост, несколько агентов, несколько человек на одном процессе.
Код к переезду готов — `ключСессии(доп)` уже читает `доп?.sessionId`,
карта `СЕССИИ` уже по ключу. Менять пришлось бы транспорт и запуск,
а не инструменты.

**Два замечания, которые данными не лечатся.** `aurora/bars` — тёмный
акцент подписи поверх поля. `retro/cycle` — подпись квадранта подходит
к кольцу ближе 10px на длинном тексте. Оба видны в `npm run data`,
оба свойства самих стилей. Лечится правкой стиля, а не элемента,
и требует отдельного решения — менять ли стиль ради одного элемента.

**Точность измерения текста.** Ширина считается как
`длина × кегль × 0.55`; для кириллицы это грубо, отсюда пограничные
замечания вроде 15.9px при норме 16. Точность даст только таблица метрик
Inter. Работа небольшая, но затрагивает все 52 элемента разом и потому
не должна идти вперемешку с переводом на данные.

## Порядок и почему такой

Задача 1 идёт первой, хотя выглядит документацией. Она делает `describe`
пригодным для того, ради чего он написан, и заодно даёт числа, которые
понадобятся в задачах 2–7 при подгонке.

Задачи 2–7 отсортированы по возрастанию риска: сначала подстановка строк,
где сломать нечего, потом позиционирование, потом пересчёт геометрии,
последним — `trans`, которому нужен новый договор.

Задача 8 идёт последней намеренно: правило роста проще применять, когда
все элементы уже принимают данные и видно, какие ряды бывают переменными.
