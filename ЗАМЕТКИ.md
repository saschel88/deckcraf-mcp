# Заметки к продолжению

Состояние на 27 августа 2026, конец сеанса. Писалось, чтобы продолжить
с любой машины, не восстанавливая контекст.

## Где остановились

**Перевод на данные закончен.** Все 52 элемента принимают данные пользователя,
подстраивают под них разметку и **говорят, что с этими данными сделали**.
Последнее появилось только что, задачей 12: до неё элемент, увидевший беду
в данных, молчал.

Работа идёт в ветке `elements-data`. `main` остаётся на `68c12ec` и не тронут —
слияние впереди, после того как закроются задачи качества.

```bash
npm install
npm run check     # 676 пар, сличение с эталонным слепком
npm run data      # данные доходят до разметки, элемент подстраивается
npm run smoke     # сервер настоящим MCP-клиентом по stdio
npm run host      # условия запуска: чужой каталог, урезанное окружение
npm run find      # качество поиска на 40 запросах
npm run diff      # что изменила правка: две версии кода на одном движке
npm run schema    # обход по веткам ответа: 6243 вызова против объявленных схем
npm run baseline  # ПЕРЕСОБРАТЬ слепок — только по отдельному решению
```

Все обязаны заканчиваться без сбоев. `npm run check` обязан сказать
**«разошлись 0»**.

**Кроме одного места, и оно временное.** `npm run smoke` сейчас красный
нарочно: **проверок 146, сбоев 5**. Наряд 30 завёл пробу ВПЕРЁД правки — весь
машиночитаемый ответ у нас лежит одним блоком `type: 'text'`, тогда как в
протоколе для него есть штатное место (`structuredContent` + `outputSchema`,
MCP от 2025-06-18). Пять красных строк — в конце вывода `smoke`, они и
называют, чего не хватает. Позеленеет наряд, который допишет схемы шести
инструментам и продублирует ответ в `structuredContent`, НЕ трогая `content`.
Разбор — в `.superpowers/sdd/PLAN/task-30-proba-report.md`. Пока эта строка
здесь стоит, красный `smoke` с пятью сбоями — ожидаемое состояние, а шесть
и больше — уже нет.

### Чем какая проверка занимается и чего не ловит

| проверка | что доказывает | чего НЕ ловит |
|---|---|---|
| `check` | 676 образцовых отрисовок совпали со слепком побайтово | **всё, что происходит на переданных данных**: слепок снят без них |
| `data` | переданное дошло до разметки, подписи не потерялись, пределы держатся | изменение разметки, которое ни одну подпись не потеряло |
| `smoke` | сервер отвечает по stdio так, как обещает договор | саму картинку: `smoke` смотрит на ответ, а не на разметку |
| `host` | запуск из чужого каталога и с урезанным окружением | всё содержательное |
| `find` | поиск находит нужный элемент на 40 запросах | всё, кроме поиска |
| `diff` | **что именно изменила правка** — две версии кода рядом на шести наборах данных | ничего не сторожит: не говорит, правильно ли изменилось, только что |
| `schema` | ответ сходится с объявленной `outputSchema` **на всех ветках**, 6243 вызова | смысл ответа: сверяет форму, а не правду |

`check` и `diff` — не замена друг другу, а два разных вопроса. `check`
сторожит образцы от сдвига и краснеет сам. `diff` не сторожит ничего: он
показывает, что изменилось, а решает человек.

**Внимание тому, кто ставит `diff` в цепочку `&&`.** Найденные расхождения —
это нормальный ответ, и код выхода у него **0**. Ненулевой код `diff` даёт
ровно в четырёх случаях, и все четыре означают «мне нельзя верить»,
а не «правка плохая»:

```
движок менялся между ревизиями        (снимается --engine-changed-ok)
стенд не поверен                       (не снимается ничем)
набор данных отпал — покрытие потеряно (не снимается ничем)
--expect не сошёлся множествами
```

Цепочка приёмки сторожит достоверность прогона, а не содержание правки.
Содержание читает человек.

## Чего `npm run check` не доказывает

Это самое важное, что выяснилось за день, и оно ломает привычку.

**`check` сличает 676 отрисовок, и все 676 отрисованы БЕЗ данных.** Он говорит
ровно одно: образцы не поехали. Всё, чем проект занимается с первого дня —
поведение элемента на переданных данных, — этой проверкой не покрыто вовсе.

Наряд 12 изменил 383 строки в `src/elements.js`. Зелёный `check` об этом
молчал. Доказательство собрал проверяющий вручную: поднял старый и новый
`src/elements.js` рядом на один `src/engine.js` и сличил выводы побайтово —
**4564 сравнения**, 52 элемента × 13 стилей в шести режимах (без данных,
со сборщиком, договорные данные, «мутант» с числами `×3+7` и удлинёнными
строками, «всё в минус»), расхождений 0.

**Правишь поведение на данных — гоняй `npm run diff`.** Сличать надо две
версии кода между собой на одних данных, а не код со слепком: такой прогон
не хранит эталона и потому не устаревает. За три дня его собирали заново
четырежды (наряды 12, 16, 17а, 20), и каждый раз он умирал вместе с временным
каталогом проверяющего. Теперь он живёт в `tools/diff-run.js`.

```bash
npm run diff                     # рабочее дерево против прошлого коммита
npm run diff -- <рев>            # против любой ревизии
npm run diff -- <стар> <нов>     # две ревизии между собой
npm run diff -- <стар> <нов> --expect <список>   # сверить множество пар
```

Шесть наборов данных на каждую пару стиль/элемент: образцы, образцы
со сборщиком замечаний, договорные данные `data-check`, все строковые слоты
забиты фразой по объявленному в договоре пределу, «мутант» (числа `×3+7`,
строки удлинены) и «всё в минус» (числа `−|v|−13`). Итого **4056 сравнений**
на прогон, и число сравнений печатается всегда: «расхождений 0» без него
не значит ничего, потому что ноль сравнений тоже даёт ноль расхождений.

Три вещи, которые он делает сам, потому что на них спотыкались руками:

- **сторожит движок.** Старый `elements.js` поднимается на нынешний
  `engine.js` — так и надо, это изолирует правку в элементах. Но если между
  ревизиями менялся сам `engine.js`, сличение врёт, и об этом он говорит
  вслух и возвращает ненулевой код.
- **поверяет оба стенда.** Каждая сторона прогоняется против эталона СВОЕЙ
  ревизии и обязана дать «разошлись 0». Правленому дереву эталон не ровня —
  для него заведена вторая поверка, независимая: тот же эталон меряет
  отдельным процессом штатный `audit.js`, и его «разошлись N» обязано
  совпасть с тем, что дал режим «образцы». Число поверенных стендов
  печатается всегда: «стендов поверено N из 2». Инструмент, который всегда
  говорит «0», неотличим от исправного.
- **не теряет покрытие молча.** Роспись из шести наборов объявлена заранее
  и не выводится из того, что удалось собрать. Набор, который не построился
  или дал ноль сравнений, числится ОТПАВШИМ, называется в вердикте с причиной
  и роняет прогон. Ноль сравнений даёт ноль расхождений так же исправно,
  как совпадение, — и итоговая строка больше не может сказать «ни на одном
  наборе», когда наборов было меньше шести.
- **называет набор стилей.** Тринадцать, `iso` исключён. Наряд 20 намерил
  «364 пары» по четырнадцати и получил расхождение с нарядом 19 на пустом
  месте.

Ничего не пишет: ни эталон, ни рабочее дерево не трогает, временных файлов
не создаёт, из сети не тянет — старый код берёт `git show`.

**Что он ловит, а `check` нет — измерено.** В `ряды()` подсажена правка,
теряющая последний переданный ряд. `npm run check` — «пар 676, разошлись 0».
`npm run diff` — на образцах 0, **на данных 572 расхождения в 44 элементах**,
первой же строкой «`flat/bars` узлов текста 11 → 9».

## Главное правило работы с elements.js

Функции отрисовки принимают данные вторым необязательным аргументом, третьим —
собирающий массив для замечаний. **Без данных разметка обязана совпадать
побайтово с прежней.** Гарантию держит `tools/baseline.json` — слепок sha1
всех 676 пар.

**Пересборок за проект шесть, все по отдельному решению, всего 63 пары:**

| что менялось | пар | кто решал |
|---|---|---|
| `chip()` перестал рисовать зашитое «+23%» | 1 | владелец, спрошено явно |
| `column` — шкала от максимума вместо пола в 100 | 13 | владелец, спрошено явно |
| `area` — то же самое | 13 | ведущий, по прецеденту `column` |
| `micro` — зазор подписи | 11 | ведущий, заранее |
| `divider` — нижний отступ | 13 | ведущий, заранее |
| `agenda` — последняя позиция палитры | 12 | ведущий, по прецеденту |

**Седьмая и восьмая — 25 августа, обе разрешены владельцем отдельно:**

| что менялось | пар | как спрашивали |
|---|---|---|
| мера текста: регистр, зазор, ширины (задача 16) | **137** | дважды: «чиним ли вообще» на 102 парах, потом «чиним ли на 137», когда замер уточнился |
| пол ужатия кегля — 9 (задача 17а) | **124** | план обещал «эталон не задет»; замер после задачи 16 дал 124, исполнитель остановился |

Две пересборки за день — больше, чем за всё прежнее время (63 пары шестью
подходами). Обе прошли по одному порядку: **список пар записан ДО правки**,
затем правка, затем дифференциальный прогон, затем сверка списка множествами,
и лишь потом `npm run baseline` — один раз.

Порядок, который сложился и работает: исполнитель доводит правку до состояния
«эталон разошёлся ровно в тех парах, что ожидались», называет их **поимённо**
и останавливается. Пересобирает после сверки списка. Разошлась чужая пара —
разбираться, а не узаконивать.

## Соглашение об образцах

Образец каждого элемента лежит в константе `ОБР_<ид>` рядом со своей функцией
и подключён к каталогу `CATS` четвёртым элементом кортежа. Пятым идёт
необязательная оговорка — она есть только у `timeline`.

**Литерал не должен встречаться дважды.** На этом спотыкались десять раз:

```bash
grep -n "==null?'[^']\+'" src/elements.js    # запасное значение в тернарнике
```

и глазами: строка из `ОБР_x`, встретившаяся в теле той же функции второй раз.
Цвета берутся из стиля `s` и в образец не входят.

**И в пробах тоже.** Проба наряда 28 держала образцовое «Убрать» литералом
и покраснела бы на ВЕРНОЙ правке `ОБР_m22`. Ожидаемое значение берётся
из каталога — `CATS`, четвёртый элемент кортежа, — а не переписывается
в пробу: ожидание обязано ехать за образцом.

**Порядок внутри образца бывает частью договора.** У `m22` и `swot` места
квадрантов закреплены за смыслом, и `композиция.порядок_слотов` называет
их поимённо. Сторожат пробы по координатам: середина считается по самим
четырём точкам, левое-правое и верх-низ — от неё, никаких зашитых чисел.

## Что читать первым

`PLAN.md` — двадцать три задачи, закрыто двенадцать. У каждой сказано, **чьё она лицо**: человека
с задачей, модели-сборщика, хоста, ведущего или зрителя слайда. Если лицо
назвать нельзя, задача, скорее всего, лишняя — так с плана сняли
HTTP-транспорт.

`ТЗ.md` — постановка и раздел «Кто пользуется сервером» с разбором пяти
действующих лиц. Главное правило: **человек инструментов не видит никогда**.
Узнал слово `fmatrix` — конструкция сломалась.

Наряды и отчёты лежат в `.superpowers/sdd/PLAN/` — каталог git-игнорируется,
но переживает сеанс. Ход работ по нарядам — `progress.md` в той же папке.

## Порядок работы над нарядом

Сложился к 27 августа, введён по замечанию владельца и окупился на первой же
задаче.

```
замер (шаг 0) → проба → правка по швам, параллельно → ревизия независимыми стендами
```

**Шаг 0 — замер с остановкой перед правкой.** Сработал восемь раз за пять
дней, и каждый раз по делу: числа, записанные в план раньше, оказывались
мёртвыми после промежуточных правок. «Эталон не задет» превратилось в 124
пары; 102 пары — в 137; «45 полей» — в 78.

**Проба идёт отдельным нарядом впереди правки, и правящему запрещено её
трогать.** Считает, что проба неверна, — обязан остановиться и доложить.
Раньше проба и правка жили в одном наряде, и у исполнителя всегда была
возможность подвинуть ожидание; теперь её нет по устройству. Проверяется
побайтовым сличением при ревизии.

**Пробу поверять с обеих сторон.** Мало показать её красной: надо собрать
патченую копию и убедиться, что она **позеленеет от верной правки**, и что
она **кусается** на мутациях. Заведомо безвредная мутация должна оставить
её зелёной — иначе проба срабатывает попусту.

**Ревизия — независимыми стендами.** Один стенд на девять пунктов идёт час
и не распараллеливается. Два стенда нашли **разное**: один — что сторож
не в приёмке, другой — что схема молчит там, где могла бы сторожить.

**Наряд дробить по естественным швам.** Сто шесть мест правки одним заданием
слишком крупно: швы были — замер, простые места, трудные имена.

## Что осталось в плане

**30б** — разметить ответ по `annotations.audience`. Закрывает половину
слепой зоны «что видит человек, что видит модель».

**31** — 146 мёртвых определений в `<defs>`, 27 190 байт, 100 файлов из 676.
**Требует десятой пересборки слепка — решение владельца.**

**33** — третий источник об исключённых парах: прозаическое поле `стили`
объявляет 18 пар против восьми и расходится с заметками у семи элементов
из двенадцати. **Требует суждения владельца:** дописать причину значит
сочинить довод за него, вычеркнуть — стереть его суждение.

**32** — поверить нашу меру текста браузером. Её точность изнутри проекта
не мерили ни разу; mermaid меряет `getBBox()` в headless Chromium — тем же,
чем рисует зритель.

**23** — «массив массивов» не сверяется **и говорит неверное**: при матрице
4×6 сервер сообщает «лишние не попадут», а шестая подпись рисуется. Второй
случай того же рода: `heat.matrix` объявлен размером, а не количеством,
и потому не меряется вовсе — матрица 7×8 режется до 5×6 молча.

**22** — образцы нарушают наш же договор. Отложена владельцем, замеры
сохранены: четыре образца стоят 52 пары, все шестнадцать — 208 и дают
многоточий 124 → 0.

**Остаток задачи 29, решение владельца:** три места (`wf.steps`,
`radar.axes`, `roadmap.rows`, 48 записей) говорят о переполнении **образца** —
тот же род лжи, от которого правка защитила 106 новых мест. Не тронуты:
отнять существующие записи не аддитивно.

**17б** — режимы автофита, лестница ступеней, отбрасывание хвоста.

**9** — шкала дельты в `retro`. Наряд написан, лежит готовым.

**13** — сплошная сверка композиции, около 5% чисел неверны.

**11** — путь Claude Design. Никем не проверялось.

**21, вне очереди** — вместо обрезки предлагать другую фигуру. Мысль
владельца, подтверждённая живым прогоном: содержание потерялось молча,
и наши проверки не могли этого увидеть по устройству.

**Словарное, из задачи 10.** Промахов поиска 8 из 40.

**Открытый вопрос: `iso` — мёртвый стиль.** В `ST` четырнадцать,
у пользователя тринадцать. На договор не повлиял, но замеры перекашивал.

## Что выяснилось и стоит помнить

**Поле, отдаваемое несколькими инструментами, — это несколько договоров.**
`не_рекомендуется_в_стиле` выходил из `catalog` строкой и без ключа на годной
паре, а из `find` и `describe` — объектом и `null`. Источник был один,
ни одно поле не лгало — расходилась ФОРМА. Клиент, научившийся читать
`.почему` на одном инструменте, получал `undefined` на другом. Тот же дефект,
что чинил Д-2, только заведённый не внутри ответа, а между ответами.

**Проба, сличающая наличие поля, не сличает его форму.** Три инструмента
проверялись тремя отдельными утверждениями, и каждое по отдельности
проходило. Ловится это только пробой, которая берёт все ответы РАЗОМ
и сравнивает их между собой.

**Инвариант в пробе без письменного основания — это соглашение, а не
договор.** Проба закрепила «доминанта — это `pos`», а договор объявлял
доминантой только МЕСТО. Спорить было не с чем — и заметьте, токен,
который НАЗЫВАЕТСЯ акцентным (`ac`), лежит вовсе на другом квадранте.
Лечится тем же ходом, что и порядок слотов: договор называет роль
(`m22.композиция.роли_цвета`, словарь ролей — `spec/tokens.json`),
а проба её читает. Ни одного цвета и ни одного имени токена в пробе.

**Стиль сессии не снимается.** После `start` вызов `catalog` без аргументов
всё равно видит сессионный стиль. Проба «без стиля» имеет смысл только
до первого `start` — иначе она утверждает невозможное. Поймано собственным
красным прогоном, а не рассуждением.

**Сторожа надо проверять мутацией.** Договор, объявленный доминантой
`accent`, обязан ронять пробу — и роняет, на двенадцати стилях. Проба,
которую не видели красной от ПОДСАЖЕННОГО расхождения, сторожит
неизвестно что.

**Концы строк — свойство файла, а не проекта.** В репозитории есть и LF
(`smoke.js`, `checks.js`, `styles.json`), и CRLF (`index.js`, `elements.js`,
`elements.json`). Вставка «своими» концами делает файл перемешанным,
и построчный разбор такого файла врёт: `split` по CRLF дал 200 «строк»
вместо 1113. Проверять до правки, а не после.
**Пересборка эталона сверяется МНОЖЕСТВАМИ, а не числом.** Девятая по счёту,
и порядок тот же: список пар пишется в файл ДО правки, после правки
расхождения сверяются с ним на «сверх списка» и «из списка, но не
разошлось», и только потом `npm run baseline` — один раз. Список читается
из файла, а не из памяти. У задачи 28 сошлось: 13 против 13, пропавших
и новых ключей ноль. Совпадение чисел ничего не доказывало бы: тринадцать
не тех пар — тоже тринадцать.

**Проба, зелёная по обе стороны правки, — не пустая, если сторожит
обещание.** У m22 проба про канон была красной до и зелёной после — она
ловила ошибку. Проба про доминанту зелена в обоих состояниях, и это её
работа: договор объявляет доминантой «квадрант сделать», и она запретила
бы вариант, где порядок починен, а акцент уехал на чужой квадрант.

**Сторож, смотрящий на один стиль, сторожит один стиль.** Цвет квадранта
задаётся поштучно по тринадцати стилям; проба, проверявшая доминанту
только в `flat`, пропустила бы правку в любом другом. Расширена на все.

**Ожидание в пробе обязано ехать за образцом.** Проба про подстановку
знала «Убрать» литералом — и покраснела бы на ВЕРНОЙ правке ОБР_m22.
Теперь берёт ожидаемое из самого каталога. Литерал, повторяющий образец,
— тот же дефект, что литерал, повторяющий образец в теле элемента.

**`brutal` ахроматичен: все четыре цветовых токена #000000.** Отсюда
13 расходящихся пар в режиме «образцы» и 12 в наборах с данными —
перестановка цветов там не меняет ни байта. Исключать такой стиль надо
замером (числом различимых токенов), а не именем в списке: перестанет
быть ахроматичным — войдёт в проверку сам.
**Сторонняя проверка видит то, чего не видим мы, и это устройство, а не
везение.** Наши проверки сторожат данные, ДОШЕДШИЕ до элемента. Клиент
смотрит на обещание против поведения. Владелец 27 августа прогнал полсотни
случаев чёрным ящиком и нашёл шестнадцать дефектов, три высоких, — все три
в этой слепой зоне.

**Подстановка образца — общий приём библиотеки, и она была молчаливой.**
Замер наряда 28: 250 случаев, о 160 не говорилось ничего — 51 элемент,
144 пары «элемент × поле». Самая широкая дыра, которой не было даже
в отчёте владельца, — **пустая строка**: `строка()` приравнивает `''`
к «не передано», а `сверитьДанные` видит строку верного типа в пределах
длины и молчит по праву. 90 случаев, ни слова ни в одном. Теперь говорит
запись `дополнено_образцом`; отрисовка не тронута, эталон стоит.

**Сличать данные с разметкой дешевле, чем править полсотни функций.**
Подстановка сидит в общих `ряды()`/`строка()`, а имени слота они не знают.
Сервер знает и образец, и переданное, и готовую разметку: путь строкового
листа образца сразу даёт имя слота. Мера ошибается в сторону молчания —
узел, похожий на переданное значение, не разбирается вовсе. Ложное
обвинение дороже пропуска.

**Одно и то же слово в двух полях ответа — это два ответа на один вопрос.**
`describe` отдавал «исключён» в заметках и `доступен_в_стиле: true` рядом.
Лечится не выбором победителя, а разведением вопросов: «отрисует ли draw»
и «считает ли библиотека пару годной» — разные вопросы, и у каждого свой
источник. Исключение переехало в машиночитаемый ключ `не_рекомендуется`;
прозу сервер не разбирает — здесь мягкий разбор ломался дважды.

**`ALLOW` сегодня не запрещает ничего.** Она заполнена только для шести
переходных слайдов, и в каждой записи перечислены все тринадцать стилей.
Значит `доступен_в_стиле` — константа `true` на все 676 пар. Об этом легко
забыть и построить на поле вывод, которого оно не несёт.

**Смысловую ошибку пробой не поймать, а её причину — можно.** Зеркальный
Эйзенхауэр не ловится ничем: разметка валидна, предупреждений нет. Зато
ловится разрыв между договором и кодом: порядок слотов записан в договоре,
и проба сличает его с отрисовкой по координатам подписей. Ошибка стала
выразимой — раньше вопрос «где стоит слот [3]» не имел письменного ответа
нигде, и спорить было не с чем.
**Живой сервер держит код на момент запуска.** Node кэширует модули. После
каждого закрытого наряда — `/mcp` и переподключить `svg-library`, иначе
проверяете вчерашний код.

**Предельный вклад — не то же самое, что собственный.** В задаче 16 зазор
поверх исправленного капса давал 44 поля из 58, а сам по себе — три. Вклады
не складываются: в капс-стиле занижений два, и, убрав одно, мера всё равно
занижает, подгонка не срабатывает. Числа «сверх ступени X» без ступени X
не пересказывать.

**Догадку надо мерить, а не обосновывать.** «Капс без таблицы ширин
бессмыслен» звучит убедительно; исполнитель вместо рассуждения проверил,
меняет ли `toUpperCase()` длину строки — на 8658 строках библиотеки не меняет
ни у одной. Получилось тождество, а не мнение.

**Одно понятие — одно определение на весь наряд.** В шестнадцатом ведущий
мерил прописную как `/[А-ЯЁ]/`, а в тексте наряда написал `/[A-ZА-ЯЁ]/`.
Семь пар расхождения из воздуха.

**Проверка, которая печатает ноль, потому что не смотрела, — главная беда
этого проекта.** За один день нарвались пять раз:

```
опечатка [-d.] вместо [-\d.]      проба меряла с нулевым зазором и была зелена
                                   при любом его значении
сторож искал число вхождением      остался бы зелёным при подмене девятки тройкой
подстроки
ворота «есть ли число в длине»     три строковых поля молча не проверялись,
                                   проба печатала «нарушений 0»
те же ворота у массивов            тринадцатое поле выпадало так же
сломанный разбор данных в diff     две трети покрытия отпали, вывод сказал
                                   «расхождений 0 ни на одном наборе», код 0
```

Отсюда правило: **число сравнений называть всегда**. «Расхождений 0» без него
не значит ничего — ноль сравнений тоже даёт ноль расхождений. И перебор строить
**по типу поля**, а не по наличию числа: поле без разбираемого предела обязано
ронять пробу, а не выпадать из неё.

**Нестрогий разбор подхватывает чужую цифру.** `/(\d+)/` по строке «в самом
тесном из 13 стилей» дало предел 13 полю, у которого он 26. Якорить:
`^до (\d+) знак`.

**Предел в договоре — не ограничение, а обещание.** Предел, при котором текст
обрезается, — ложное обещание: модель уложится в него и всё равно получит
многоточие. И одно число не может обслуживать тринадцать стилей: заголовок
держит 44 знака в `flat` и 26 в `line`. Считать под тот стиль, который уже
выбран, — `describe` его знает.

**Сторонняя проверка чёрным ящиком видит то, чего изнутри не видно.**
Владелец написал план сам и прогнал его, не открывая исходников: шестнадцать
дефектов, три высоких. Причина проста — **наши проверки сторожат данные,
дошедшие до элемента, а клиент смотрит на обещание против поведения.**

**Живой вызов своего же сервера — тоже проверка.** Разведка вызвала `draw`
с подписью в 70 знаков и увидела обрезанный текст при пустом списке
замечаний. Из этого вышла задача на 106 молчащих мест.

**Путь воспроизведения через настоящего клиента может отличаться от прямого
вызова.** Проба на `Infinity` не проходила через MCP-клиента: он сериализует
аргументы, а `JSON.stringify(1e400)` сам даёт `null` — литерал до сервера
не доезжал. Проба осталась красной **после** правки, и это её и выдало.
Там же: `JSON.stringify(±Infinity)` даёт `null`, поэтому нефинитные значения
уходят в ответ строкой.

**Шрифта Inter нет ни в системе, ни в репозитории**, хотя разметка объявляет
именно его. Рисуется запасной — Segoe UI или SF Pro, смотря у кого. Значит
мера обязана **завышать** ширину: пусть текст ужмётся раньше, чем вылезет.

**Сомнения исполнителей ценнее их основной работы.** Настоящие дефекты
находились именно так: выдуманное число в `retro`, чужой ответ под чужим
вопросом в `faq`, плоские графики у `column` и `area`, пустой пример у `vec`,
ложное предупреждение, палитра, кончавшаяся приглушённым серым.

**Проба, которую не видели падающей, ничего не гарантирует.** Проверка имён
инструментов покрывала половину нужного и пропустила ошибку, уронившую весь
сервер вместе с чужими MCP-серверами сессии: шаблон
`^[a-zA-Z0-9_.-]{1,64}$` проверяет **не только имена инструментов, но и каждый
ключ в `input_schema`** — кириллический ключ роняет весь список инструментов
четырёхсотым ответом.

**Пересказ договора своими словами расходится с договором.** План шесть раз
ошибался в типах и формах полей. Источник истины — `spec/elements.json`.

**`git commit` без указания путей коммитит весь индекс.** За проект чужая
работа попадала в чужой коммит четырежды. Верная форма — `git commit -F <файл>
-- <пути>`, и заметьте порядок: `-F`/`-m` идут **до** `--`.

## Два замечания, которые данными не лечатся

`aurora/bars` — тёмный акцент подписи поверх поля. `retro/cycle` — подпись
квадранта ближе 10px к кольцу на длинном тексте. Оба видны в `npm run data`,
оба свойства самих стилей, а не элементов.

## Ход работ страницей

https://claude.ai/code/artifact/d75cea91-6f33-468c-a271-080efad54521 — задачи,
числа и находки. Отдельно про кегль на слайде:
https://claude.ai/code/artifact/fb1a4c7b-74e0-4eca-91cf-04de53cb5f83
